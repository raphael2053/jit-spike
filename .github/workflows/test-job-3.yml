name: Test Job 3
on:
  workflow_dispatch:
    
jobs:
  test-3:
    runs-on: [self-hosted, jit-spike]
    steps:
      - name: Identify Job
        run: |
          echo "=========================================="
          echo "This is TEST JOB 3"
          echo "Job ID: ${{ github.job }}"
          echo "Run ID: ${{ github.run_id }}"
          echo "Runner Name: ${{ runner.name }}"
          echo "=========================================="
          
      - name: Wait and verify
        run: |
          echo "Running for 30 seconds to allow observation..."
          sleep 30
          echo "Test Job 3 Complete!"